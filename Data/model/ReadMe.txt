# A first-level heading Pretraining and finetuning model information should be stored here after running mlm_pretrain.py or binding_finetuning.py

This folder should contain
- train and validation losses for (hyperparameter tuning across epochs)

Note: Model Weights (.pt) are too large to be stored here (run the model to get the weights)
